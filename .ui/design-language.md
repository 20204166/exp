# Phase 7 UI Design Language

The Thermals surface uses the existing Tkinter dashboard language: ttk page
shells, shared color/font tokens, compact section cards, left-aligned labels,
and bounded Canvas graphs. The fix preserves the existing warning and accent
colors and adds only a distinct critical threshold dash pattern.

No new visual language, dependency, global token, animation, or business
behavior was introduced.
