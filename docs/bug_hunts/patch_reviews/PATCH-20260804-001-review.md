# PATCH-20260804-001 — Mode A patch review

## Mode and risk

BugGuard mode: Mode A — code-edit validation
Risk level: high / contradictory
Validator threshold: full A4
Reason: A Mode C performance patch exposed correctness, lifecycle, contract, and Windows native-boundary failures; this repair must pass fresh full adversarial validation before returning to Mode C.

## Patch summary

Repair the performance patch without changing public behavior except for the explicitly requested Windows Known Folder resolution and Recycle Bin sizing; preserve exact analyzer defaults, fail-soft filesystem scanning, stable candidate output, process snapshot identity, cache correctness/bounds, cooperative cancellation, Tk teardown behavior, protected-process checks, and Trash-only deletion.

## Original bug / task evidence

Original task: improve Windows responsiveness and compatibility while preserving macOS/Linux/NVIDIA behavior and all safety/public contracts. Mode C review `PERF-20260804-001` identified stale hash reuse, unbounded cache retention, traversal iterator-error propagation, analyzer delegation incompatibility, PID-joined process snapshots, incomplete cancellation/close fencing, unhandled Tk `RuntimeError`, missing ctypes prototypes, and known-folder fallback scope drift.

## Diff summary

Current working-tree repair touches `algo.py`, `maintenance/actions.py`, `maintenance/dialogs.py`, `maintenance/scanner.py`, `tests/test_maintenance.py`, `tests/test_storage_dialog.py`, `tests/test_window.py`, and `window.py`. The performance opposition artifact is `docs/bug_hunts/performance_reviews/PERF-20260804-001-opposition.md`.

## Tests and checks run

- `python -m unittest discover -s tests -v`: 41 passed, including the user's unwired future-use component tests.
- `ruff check .`: passed.
- Changed-file `ruff format --check`: passed.
- `python -m compileall -q algo.py window.py maintenance tests`: passed.
- `git diff --check`: passed.
- Native Windows read-only smoke: known Downloads folder resolved and Recycle Bin size returned an integer.

## Static/type/format checks

`mypy` and `pyright` are installed but remain non-clean because the repository has no type configuration and optional third-party stubs are unavailable (`psutil`, `pynvml`, `send2trash`), plus existing Tk/test typing diagnostics. No new application type error was accepted without review.

## Selected validators

Full A4: Opposing Agents 1-4 plus Agent 5. Portable independent fallback is required if registered agents cannot write directly to this artifact.

## Validator findings

### Opposing Agent 1 — Patch/Reproduction Skeptic

**Fresh portable review status:** I independently reread the current working-tree diff, this complete patch-review artifact, the original task evidence recorded in its summary, and the complete `docs/bug_hunts/performance_reviews/PERF-20260804-001-opposition.md`. This is a final portable Validator 1 pass, not an Agent 5 audit. No application code or normal test was edited; probes used temporary files, deterministic doubles, live read-only system calls, and a bounded child process.

**Evidence and scope checked:**

- The current surfaces are `maintenance/scanner.py:127-245` for dashboard collection, `:247-343` for process enumeration, `:352-480` for Downloads traversal, `:494-585` for duplicate classification, `:703-790` for marker/hash reuse, `:792-819` for GPU fallback caching, and `:930-1009` for Downloads-root resolution.
- `scan_processes()` still enumerates live processes and samples live CPU values on every call; it has no result cache. `scan_dashboard()` still performs live psutil, GPU, and Trash work on every call; a faster repeat is not a dashboard-result cache. The duplicate cache is per `SystemScanner` instance, and its warm path still computes a complete content marker for each candidate.
- The latest repair at `scanner.py:378-406` avoids new keywords on default private-hook calls and retries legacy one-argument traversal/reason hooks on the optional path. The latest fallback at `scanner.py:946-955` selects a new sentinel name when occupied rather than reusing an existing path.

**Current validation commands and results:**

- `python -m unittest discover -s tests -v` -> `Ran 39 tests in 0.704s`, `OK`.
- `ruff check .` -> all checks passed.
- Requested scoped `ruff format --check` over the eight changed files -> `8 files already formatted`.
- `python -m compileall -q algo.py window.py maintenance tests` -> exit 0 with no diagnostics.
- `git diff --check` -> no whitespace errors; Git emitted only the existing LF/CRLF warnings.
- A separate whole-repository format check remains non-clean for unchanged `main.py`, `maintenance/__init__.py`, and `maintenance/models.py`; no formatting command was run in write mode and no whole-repository format pass is claimed.

**Latest private-hook, sentinel, and cache-bound probes:**

- Replacing `_download_file_stats` and `_download_reasons` with old one-argument hooks, then using the optional progress/cancellation scan path, returned one candidate without an unexpected-keyword error. The current fallback behavior preserves these private shapes for this probe.
- With `Downloads`, `Downloads.__unavailable__`, and `.1` occupied by regular files, `_safe_downloads_fallback()` returned `Downloads.__unavailable__.2`, which did not exist and was not a file or directory. The latest sentinel collision was not reproduced.
- A populated cache was pruned from `2` entries to `0` after the scan root was removed. A 1,025-file fixture retained exactly `1024` entries, matching `HASH_CACHE_MAX_ENTRIES`.

**Full-content marker and cache-result challenge:**

- A temporary fixture containing two identical 8 MiB files returned one candidate on every scan. Five fresh scanner instances had median `0.685s` (`0.7498, 0.5488, 0.6850, 0.5342, 0.7349`); five repeats on one scanner had median `0.4362s` (`0.4345, 0.3884, 0.5613, 0.4362, 0.4542`). These are scanner-cache-cold versus scanner-cache-warm labels only, not cold OS cache, cold disk, cold interpreter, or cold process measurements.
- The first scan recorded `marker_calls=4`, `marker_bytes=33554432`, `hash_calls=2`, and `hash_bytes=16777216`. Across five warm repeats it recorded `marker_calls=20`, `marker_bytes=167772160`, and `hash_calls=0`. Each warm scan therefore skips SHA-256 work but still reads both 8 MiB candidates twice through the full-content BLAKE2b marker path, 32 MiB per scan.
- A deterministic counter-test mutated one candidate after its second marker and before the cache-hit return. The scan still returned the other file as the old duplicate (`FINAL_MARKER_RACE returned_old_duplicate=True`). This is a deliberately controlled timing window, not a claim that ordinary scans are atomic; it proves the final marker/stat check is not atomic with returning the cached digest. Under the patch's content-validation/cache contract, this remains a P1 result-integrity blocker because a changed file can be classified from an old digest and presented as a reversible-Trash candidate.
- The ordinary same-size/same-timestamp rewrite test and path/cache-bound probes pass. The remaining race is after the last validation read, not the older metadata-only invalidation case.

**Static fallback versus NVIDIA challenge:**

- A deterministic Windows static fallback returned the same result twice with `nvidia_calls=2`, `static_calls=1`, and a populated static cache. A deterministic non-empty NVIDIA result returned the same result twice with `nvidia_calls=2`, `static_calls=0`, and `static_cache=None`. Successful static fallback details are cached per scanner; non-empty NVIDIA results remain dynamic.
- On the actual host, `pynvml_available=True`; two GPU calls returned three detail lines in `3.1379s` and `0.0012s`, with the static cache populated after the first call. This exercises the host's fallback/cached-static path and does not prove NVIDIA-result caching.

**Dashboard and process live-work challenge:**

- Two actual `scan_dashboard()` calls returned six resources each in `13.4485s` and `0.5537s`. Wrapped read-only psutil calls ran twice each for `cpu_percent`, `cpu_freq`, `virtual_memory`, `swap_memory`, `disk_usage`, `net_io_counters`, and `sensors_battery` (`cpu_count` ran four times). The warm dashboard timing therefore reflects cheaper live sampling and static GPU reuse, not reuse of a dashboard result.
- Two actual `scan_processes()` calls took `13.2980s` and `9.5623s` and returned 103 and 101 candidates; the wrapped `process_iter` call count was 2. Counts changed between immediate calls, so there is no process-result cache or workload-stable process-speed claim.

**Live Downloads timeout challenge:**

- A child-process probe invoked `SystemScanner().scan_downloads()` on the actual default Downloads path with output suppressed and a 20-second parent containment bound. Result: `LIVE_DOWNLOADS completed=False elapsed=20.254s returncode=1`; the child was killed at the containment bound and produced no result count or completed duration.
- The containment bound is not an application timeout or completion guarantee. The prior PERF artifact also records the live path as exceeding 120 seconds both before and after, so the real-tree before/after performance claim remains unresolved. Neither timeout is a pass or a speedup.

**Workload-match challenge:**

- The two-file fixture isolates marker/hash behavior and is not representative evidence for the timed-out Downloads tree's file count, directory depth, size distribution, duplicate groups, metadata churn, or storage/cache state. It demonstrates digest-work avoidance only after paying full-content marker I/O.
- The live process comparison changed from 103 to 101 candidates between immediate calls, and the prior PERF comparison used different before/after candidate counts (`110` versus `120`). Those measurements are not workload-matched.
- The passing 39-test suite, Ruff check, scoped format check, compile check, and diff check establish execution and selected behavior coverage. They do not establish repeatable timing distributions, live-tree completion, cold-start improvement, NVIDIA caching, dashboard-result caching, process-speed improvement, or an end-to-end Windows responsiveness gain.

**Verdict:** Unsafe for unconditional Mode A acceptance because the final-marker cache race remains a reproduced P1 result-integrity blocker. The latest private-hook and sentinel collision fixes passed fresh probes, and the cache entry bound, missing-root pruning, static GPU reuse, and platform-specific NVIDIA/static precedence behaved as described. The defensible performance claim is narrow: a stable repeated scan on one `SystemScanner` can skip SHA-256 digest work after full-content marker reads, and successful non-NVIDIA static fallback details can be reused. The fresh evidence does not support a general cold-start, process-speed, dashboard-result-cache, NVIDIA-cache, live Downloads, or end-to-end Windows responsiveness claim. This section is Validator 1 only and does not act as Agent 5.

### Opposing Agent 2 — Repo-Truth Skeptic
**Fresh portable review status:** I independently reread the current working-tree diff, exact callers/tests, the original task summary in this artifact, and the complete `PERF-20260804-001` opposition. This is a fresh portable Validator 2 pass, not an Agent 5 audit. No application code or normal test was edited; probes used temporary directories, deterministic in-memory doubles, mocked native functions, and read-only Windows calls.

**Evidence and scope checked:**

- Result contracts remain in `maintenance/models.py:6-63`. Caller chains are `window.py:394-425` -> `Analyzer.dashboard_snapshot()`, `ProcessDialog.refresh()` at `maintenance/dialogs.py:339-375` -> `analyzer.process_candidates()`, and `StorageDialog.scan()` at `maintenance/dialogs.py:680-726` -> `analyzer.storage_candidates()`.
- Delegation and compatibility seams are `algo.py:270-299`, `window.py:407-425`, and `maintenance/dialogs.py:352-375,697-726`. Scanner identity, traversal, cache, GPU, and Windows paths are `maintenance/scanner.py:247-365,367-480,494-790,792-819,931-1009`; Trash validation is `maintenance/actions.py:161-215`.
- The changed tests are `tests/test_maintenance.py`, `tests/test_storage_dialog.py`, and `tests/test_window.py`. No application code or normal test was changed in this pass.

**Commands and safe probes:**

- `python -m unittest discover -s tests -v` -> `Ran 41 tests in 0.805s`, `OK` (the two additional tests are in concurrently appearing untracked files and were not edited in this pass).
- `ruff check .` -> passed; changed-file `ruff format --check` -> `8 files already formatted`; `python -m compileall -q algo.py window.py maintenance tests` -> passed; `git diff --check` -> no whitespace errors apart from existing LF/CRLF warnings.
- Targeted `mypy` -> 18 diagnostics; targeted `pyright` -> 44 diagnostics, all in the previously observed optional-library, Tk, type-configuration, and test-typing classes. No type-check pass is claimed.
- `./lr --list` and `./lr impact` were not run in this pass; the artifact records that `./lr` is unavailable in this workspace. This is not a pass.
- Read-only Windows smoke returned an existing known-folder directory and `recycle_bin_size=244114575`. No real file was moved to Trash and no process action was invoked.
- Legacy Analyzer defaults, legacy UI fallback shapes, one-argument `_download_file_stats`, one-argument `_download_reasons`, one-argument `_file_hash`, and no-argument `process_iter` all returned through their compatibility paths. The optional scan path therefore did not reproduce the former private-hook TypeErrors.
- Stable same-size rewrites with the original timestamp restored and path replacement were rejected; 1,025 duplicate files retained exactly 1,024 entries; deleting the root returned `[]` and left zero cache entries. Nested output was ordered by case-folded path and exact path; iterator `OSError` returned `{}`.
- Process probes returned one candidate from one iterator, zero candidates for changed or missing creation metadata, and `ScanCancelled` when cancellation was set during `as_dict()` or the sampling phase. A blocking iterator remains cooperative and cannot be interrupted until it returns.
- Known-folder directory -> OneDrive directory -> home directory, OneDrive file -> home directory, missing home Downloads, and home file -> unique sentinel branches passed. With the base sentinel and `.1` occupied, the result was the free `.2` path.
- Mocked native DLL-load `OSError`, `ctypes.ArgumentError`, HRESULT failure, negative/failed Recycle Bin results, and `RPC_E_CHANGED_MODE` returned fail-soft values with expected COM cleanup. Success freed known-folder memory once and uninitialized COM once.
- GPU probes showed macOS recovery after a transient failure, Linux NVIDIA precedence, Linux static-fallback recovery, and one-call Windows static-success caching. Mocked `send2trash` was reached only for a valid in-root regular file; outside-root, file-root, and `OSError` cases failed closed or returned an error.

**Contract challenges:**

```text
Contract: Public/default Analyzer behavior and legacy UI/private compatibility must remain callable.
Evidence: algo.py:275-299 omits optional keywords when the public methods use defaults. window.py:407-413 and dialogs.py:352-364, :697-709 retry legacy no-argument Analyzer methods only for the specific unexpected-keyword TypeError. The optional scanner path at scanner.py:378-406 similarly retries old one-argument traversal/reason hooks, and _cached_file_hash() retries an old one-argument _file_hash. Fresh injected probes passed all of these paths, including a no-argument process_iter fallback.
Verdict: No current Analyzer, UI, or private-hook compatibility regression was reproduced. The TypeError fallbacks are narrow enough to preserve the old shapes without changing default calls.
```

```text
Contract: Full-content cache invalidation, bounds, and missing-root pruning must be real.
Evidence: scanner.py:694-790 uses size, timestamps, device/inode, full-content markers, and a post-hash stat/marker comparison. Same-size content rewrite and path replacement returned no stale duplicate. HASH_CACHE_MAX_ENTRIES is enforced during pruning and insertion at :718-738 and :782-787; 1,025 files retained 1,024 entries. The missing-root branch at :372-376 pruned the cache to zero, and the public scan lock serialized two concurrent scans.
Boundary: A deterministic probe mutated a file immediately after the hit path's second marker and the scan returned the other file as the old duplicate. Repo truth has no filesystem snapshot/transaction, FileCandidate carries no content identity, and FileManager revalidates path/root/file state rather than a scan digest. HEAD also hashed without an atomic snapshot. I therefore record this as a residual non-atomic scan/action boundary, not a new P1 cache regression. The configured bound is an entry bound, not a byte/age policy, and direct private calls are outside the public scan lock.
```

```text
Contract: Downloads traversal and duplicate output must be fail-soft and deterministic.
Evidence: scanner.py:436-465 catches initial scandir, iterator advancement, and per-entry filesystem errors and closes the iterator. Final candidates sort by negative size, case-folded path, and exact path at :417-424; duplicate retention uses the same case-fold-plus-exact-path key at :580-585. Fresh nested, iterator-error, hidden-entry, and equal-case tie probes passed.
Verdict: No current traversal or ordering regression was reproduced. The replacement skips hidden entries and does not follow symlink directories.
```

```text
Contract: Process candidates must not mix identities, and cancellation must be cooperative.
Evidence: scanner.py:255-305 materializes one process iterator, records baseline creation times, and rejects missing or changed current creation metadata before constructing ProcessCandidate. The stable, changed, missing, no-argument iterator, and cancellation probes passed; process_iter was called once. Cancellation is checked around enumeration results, per-process work, the timed wait, and final construction.
Residual: A blocking psutil iterator remains uninterruptible until it returns, which is consistent with the cooperative Event contract. ProcessCandidate still carries only pid, and ProcessManager reopens that pid and checks current name/user/protection only; a same-PID replacement with the same values can pass action-time revalidation. That is a pre-existing action identity boundary, not a new two-pass scanner regression, and the repair must not claim end-to-end action identity.
```

```text
Contract: Every default fallback must select a usable directory or a unique unavailable sentinel.
Evidence: scanner.py:931-955 requires Known Folder and OneDrive directories, falls through to the home path, and increments sentinel suffixes while occupied. Known-folder file -> OneDrive, OneDrive file -> home, missing home Downloads, regular home-file, and regular sentinel-collision probes passed; FileManager fails closed at actions.py:196-201 for a non-directory root.
Finding: The directory predicates are not fail-soft if the filesystem raises OSError. A deterministic mock of the Known Folder Path.is_dir() raised OSError out of _default_downloads_path() instead of continuing to OneDrive/home; the same unguarded predicate exists for OneDrive and the home fallback. This is a conditional P2 fallback reliability gap, not a demonstrated Trash escape. Symlink creation was denied on this host, so broken-symlink sentinel occupancy was not runtime-tested.
Scope note: Calling _safe_downloads_fallback(Path.home()) on non-Windows changes the malformed regular-file home Downloads output relative to HEAD. Normal directory and missing-path behavior is unchanged, but this defensive change is outside the explicitly approved Windows Known Folder/Recycle Bin output scope unless separately accepted.
```

```text
Contract: Native failure handling and approved Windows output changes must be preserved.
Evidence: scanner.py:640-661 and :957-1009 set explicit argtypes/restypes, check HRESULT/negative size, catch DLL/call/conversion failures, free caller-owned memory, and balance S_OK/S_FALSE COM initialization. Mocked DLL-load, ArgumentError, HRESULT, changed-mode, success/free, and live read-only probes supported the common path.
Finding: The cleanup guard tests path_pointer.value at :1000 rather than pointer presence. A mocked SHGetKnownFolderPath failure with a non-null empty-string allocation returned None, called CoUninitialize once, but called CoTaskMemFree zero times. Microsoft documents that the caller frees the out allocation whether the function succeeds or fails. This is a conditional P2 native ownership gap for an API-variant failure; ordinary non-null paths are freed.
Scope: The successful Windows output changes observed are limited to Shell-selected Downloads resolution and the nonnegative Recycle Bin byte count, both explicitly permitted by PERF-20260804-001. No broader successful Windows output change was established.
```

```text
Contract: macOS/Linux/NVIDIA behavior and recovery must remain intact.
Evidence: gpu_details() remains macOS static-first at scanner.py:792-806, uses NVIDIA precedence on Linux/Windows, and caches only successful static details at :808-819. Fresh macOS recovery/no-NVIDIA, Linux dynamic precedence, Linux fallback recovery, and Windows static-success probes passed.
Verdict: No current GPU recovery, precedence, or platform-output regression was reproduced. Successful NVIDIA results remain live rather than incorrectly entering the static cache.
```

```text
Contract: Cleanup must remain explicitly scoped to reversible Trash operations.
Evidence: actions.py:183-209 rejects selected symlinks, resolves strictly, requires a regular file under a directory root, and calls only send2trash. Fresh valid-root, outside-root, file-root, and send2trash-OSError probes passed; no unlink, remove, rmtree, or permanent-delete API exists in actions.py. The selected-symlink probe was unavailable on this host because symlink creation was denied, but the explicit code guard was read.
Verdict: The send2trash boundary is preserved. Non-atomic scan/action timing and PID-only action identity do not bypass the root or permanent-delete boundary.
```

**Verdict:** Unsafe for unconditional Mode A acceptance because two current P2 gaps remain: filesystem predicate errors can abort the default fallback chain, and a non-null empty known-folder allocation is not freed on a native failure return. Analyzer/UI/private compatibility, stable-file cache invalidation, the 1,024-entry bound, missing-root pruning, public scan serialization, deterministic traversal/order, scan-time process identity, cooperative cancellation, ordinary native failure handling, approved Windows output scope, GPU recovery, and the reversible Trash boundary have fresh supporting evidence. The post-marker mutation probe demonstrates the existing non-atomic scanner boundary and is not counted as a new P1 cache regression without an atomic-snapshot contract. The live Downloads performance result remains unresolved in the prior PERF artifact, so no general responsiveness claim is accepted. This section is Validator 2 only and does not act as Agent 5.

### Opposing Agent 3 — Architecture/Security Skeptic

**Fresh portable review status:** This is an independent final Validator 3 pass over the current working tree, not an Agent 5 audit. I reread the current diff, this artifact, `docs/bug_hunts/performance_reviews/PERF-20260804-001-opposition.md`, the exact changed tests, current callers, and `maintenance/models.py`. No application code or normal test was edited.

**Current surfaces checked:**

- Cache mutation, retention, synchronization, traversal, and cancellation: `maintenance/scanner.py:127-778`.
- Process snapshot identity/protection and action revalidation: `maintenance/scanner.py:247-350` and `maintenance/actions.py:21-158`.
- Dashboard final fence, UI workers, action workers, close fencing, and Tk delivery: `maintenance/scanner.py:127-245`, `maintenance/dialogs.py:21-74,339-516,680-855`, and `window.py:343-603`.
- Windows Shell/COM/DLL paths and every Downloads fallback: `maintenance/scanner.py:588-649,918-993`.
- GPU recovery/precedence and Trash scope: `maintenance/scanner.py:780-807` and `maintenance/actions.py:161-215`.

**Commands and safe probes:**

- `python -m unittest discover -s tests -v` -> `Ran 39 tests in 0.701s`, `OK`.
- `ruff check .` -> passed; changed-file `ruff format --check` -> `8 files already formatted`; `python -m compileall -q algo.py window.py maintenance tests` -> passed.
- `git diff --check` -> no whitespace errors; Git emitted only the existing LF/CRLF warnings.
- `./lr --list` and `./lr impact` -> unavailable in PowerShell (`./lr` not found), not passes.
- Targeted `mypy` -> 18 diagnostics; targeted `pyright` -> 44 diagnostics. These remain non-clean for missing optional stubs, absent type configuration, existing Tk overloads, and test typing; no type-checker pass is claimed.
- Cache probes returned `CACHE_HIT_RACE False True marker_calls 2` and `CACHE_MISS_RACE False marker_calls 2`: changing a same-size file immediately after the second content marker caused both paths to return a digest that did not match the bytes after return. A 1,025-file fixture retained `CACHE_BOUND 1024 1024`; deleting the root then produced `CACHE_MISSING_ROOT_PRUNE 0`. Two public concurrent scans had `hash_calls=2` and `max_active=1`; two direct private misses had `hash_calls=2` and `max_active=2`.
- Cancellation probes returned `BLOCKING_FILE_CANCEL True ['ScanCancelled']`, `BLOCKING_PROCESS_ITER_CANCEL True ['ScanCancelled']`, `DASHBOARD_FINAL_FENCE ScanCancelled cpu_count_calls 2`, and `BLOCKING_GPU_CANCEL True ['ScanCancelled']`. The `True` values mean the blocking operation remained alive until the probe released it; the final exception then observed the event.
- Process probes returned `PROCESS_SCAN_IDENTITY stable 1`, `changed_creation 0`, and `missing_baseline 0`; cancellation set inside `as_dict()` produced `PROCESS_FINAL_CANCEL_CHECK ScanCancelled`.
- A PID-only action double terminated a same-user same-name replacement (`PID_ACTION_REPLACEMENT True (50001,)`); protected-name action was rejected. This is an action-time residual, not a changed `ProcessManager` path.
- Root probes passed known-folder directory selection, known-folder file -> OneDrive directory, known-folder/OneDrive file -> home directory, missing known-folder -> OneDrive directory, and both Windows/non-Windows home-file -> `Downloads.__unavailable__`. If that sentinel already exists as a regular file, both branches return the file. `FileManager` then rejected the file root with zero mocked `send2trash` calls.
- Native probes returned balanced success counts (`free=1`, `uninit=1`), fail-soft DLL-load `None/0`, `RPC_E_CHANGED_MODE` with no uninitialize, fail-soft `ctypes.ArgumentError`/HRESULT/negative-size results, and live read-only Windows known-folder/Recycle Bin results. `_WindowsGuid` and `_RecycleBinInfo` sizes were 16 and 24 bytes, and all required prototypes were assigned.
- GPU probes showed macOS transient-error recovery, dynamic NVIDIA precedence with no static call, recovery after a transient NVIDIA miss, and one static fallback loader call for two concurrent callers. Trash probes reached mocked `send2trash` only for a valid in-root regular file; outside-root, file-root, symlink-when available, and `OSError` cases failed closed. Windows denied symlink creation on this host, so that one runtime probe was unavailable; the explicit code guard was read.
- Lifecycle probes showed `ACTION_WORKER_AFTER_WIDGET_CLOSE False`, `COOPERATIVE_SCAN_AFTER_CANCEL False`, and `APP_CLOSE_WORKER True queued 2 destroyed True`. Late AppWindow queue items were dropped by the close fence when drained; workers were not joined. Legacy default Analyzer delegation worked, while old one-argument private `_download_file_stats` and `_download_reasons` hooks raised unexpected-keyword `TypeError` on the new optional UI path.

**Official semantics checked:**

- Python 3.12 documents explicit `ctypes` `argtypes`/`restype`, DLL lookup `OSError`, arbitrary `os.scandir` order and filesystem `OSError`, cooperative `Event` signalling, daemon-thread shutdown, and Tk calls failing when the event loop is not running: <https://docs.python.org/3.12/library/ctypes.html>, <https://docs.python.org/3.12/library/os.html#os.scandir>, <https://docs.python.org/3.12/library/threading.html#event-objects>, <https://docs.python.org/3.12/library/threading.html#thread-objects>, and <https://docs.python.org/3.12/library/tkinter.html#threading-model>.
- psutil documents cached PID-bound `Process` objects, PID-reuse limitations, `process_iter(attrs=...)`, and creation-time identity: <https://psutil.readthedocs.io/en/stable/#psutil.process_iter> and <https://psutil.readthedocs.io/en/stable/#psutil.Process>.
- Microsoft documents caller-owned `SHGetKnownFolderPath` memory and `CoTaskMemFree`, balancing `S_OK`/`S_FALSE` from `CoInitializeEx`, `RPC_E_CHANGED_MODE` failure, and `SHQueryRecycleBinW`'s HRESULT/initialized `cbSize`: <https://learn.microsoft.com/en-us/windows/win32/api/shlobj_core/nf-shlobj_core-shgetknownfolderpath>, <https://learn.microsoft.com/en-us/windows/win32/api/combaseapi/nf-combaseapi-coinitializeex>, <https://learn.microsoft.com/en-us/windows/win32/api/combaseapi/nf-combaseapi-cotaskmemfree>, <https://learn.microsoft.com/en-us/windows/win32/api/shellapi/nf-shellapi-shqueryrecyclebinw>, and <https://learn.microsoft.com/en-us/windows/win32/api/shellapi/ns-shellapi-shqueryrbinfo>.
- The official `send2trash` README documents native Trash/Recycle Bin moves and `OSError`-family failure reporting: <https://github.com/arsenetar/send2trash/blob/master/README.rst>.

**Failure-mode analysis:**

```text
Failure mode: The cache returns a digest after the file changes past the final content marker.
Evidence: The hit path captures current_stat before marker 2 and returns the cached digest after comparing marker 2; the miss path stores/returns after the same final marker. The deterministic hit and miss probes mutated a same-size file immediately after marker 2 and both returned a digest different from the bytes after return. Ordinary same-size/same-mtime rewrites are still invalidated.
Impact: A changing Downloads file can be classified with a non-atomic digest. A false duplicate can be displayed and then sent to the reversible Trash action as the wrong in-root candidate.
Verdict: Confirmed P1 result-integrity/cache-reuse blocker under the patch's content-validation contract. The broader scan/action filesystem TOCTOU is pre-existing, but this cache reuse path does not establish atomic content identity.
```

```text
Failure mode: Cache retention and concurrent cache transactions are broader than the evidence supports.
Evidence: The public _downloads_scan_lock serialized same-scanner scans and limited observed active hashers to one. The 1,024-entry cap and missing-root pruning are real. The map lock protects dictionary operations, not the private check/read/hash/store transaction; direct concurrent misses performed two hashes. There is no byte or age budget.
Impact: Public UI scans have a concrete entry bound and no cross-scanner shared cache, but private callers can redo work and retained metadata is bounded by entries rather than bytes/age. No cross-user or persistent-data leak was reproduced.
Verdict: Public behavior is supported; record P2 cache-policy/private-concurrency residuals, not a standalone P1 memory leak.
```

```text
Failure mode: Cancellation arrives during an operation that cannot observe the event.
Evidence: Checkpoints cover lock acquisition, traversal, pruning, classification, hash chunks, process loops, the timed wait, result construction, and the dashboard's final resource-construction fence. Blocking file reads, process iteration, and GPU work stayed alive after the event and stopped only after release; the final dashboard cpu_count probe raised ScanCancelled.
Impact: Cancellation and close are cooperative, not interruption or completion guarantees. No permanent action occurs in scanner cancellation paths.
Verdict: No missing final checkpoint was reproduced. Blocking-call continuation is a P2 responsiveness/lifecycle residual governed by Python Event semantics, not a patch-introduced cancellation safety bypass.
```

```text
Failure mode: UI/root teardown races workers and confirmed actions.
Evidence: AppWindow and dialogs fence delivery using close/existence/generation checks and suppress tested RuntimeError/TclError delivery failures. The close probes nevertheless left daemon scan/dashboard workers alive; AppWindow retained two late queue items until drained. Process quit/force-quit and file move call run_in_thread without an action cancellation event, so action work also remains alive after widget close.
Impact: CPU/I/O/native work and a confirmed process/file action can continue after teardown. There is no join/quiescence guarantee.
Verdict: Confirmed cooperative worker-lifecycle residual, but the worker creation/action call sites are pre-existing and the repair adds delivery fencing rather than a documented join contract. Do not classify this as patch-introduced unless close-time quiescence is an explicit requirement.
```

```text
Failure mode: Windows Shell DLL/COM failure handling is incomplete.
Evidence: The current outer lookups at `scanner.py:630-633` and `:942-950` catch DLL-load `OSError`; the inner calls catch `ctypes.ArgumentError`. Explicit prototypes, HRESULT restypes, void cleanup restypes, COM balancing, and `CoTaskMemFree` ownership all passed the fake and live read-only probes. `RPC_E_CHANGED_MODE` returned `None` without calling `CoUninitialize`.
Impact: No current DLL-load, prototype, conversion, or COM ownership failure was reproduced. Shell failure remains intentionally collapsed to `0`/`None`, which is fail-soft but not diagnostically distinguishable from empty data.
Verdict: No current native correctness blocker; failure observability is a residual P2 consideration, not the older P1 escape.
```

```text
Failure mode: The final default Downloads fallback is not directory-only.
Evidence: Known-folder and OneDrive branches require `is_dir()`, and regular files correctly fall through. `_safe_downloads_fallback` returns `Downloads.__unavailable__` for a regular home `Downloads` file, but returns that sentinel without validating it when the sentinel already exists as a regular file. Fresh Windows and non-Windows probes selected the file-valued sentinel. `FileManager` rejects it and made zero `send2trash` calls.
Impact: The resolver can construct `SystemScanner`/`FileManager` with a file root, producing an empty/unusable default scan and cleanup scope.
Verdict: Confirmed P2 directory-invariant blocker in the current repair; not a demonstrated Trash escape.
```

```text
Failure mode: A process candidate is safe during enumeration but action-time PID identity is not bound to the selected snapshot.
Evidence: The repaired one-iterator scan requires numeric baseline/current creation times and rejects changed or missing values (`stable=1 changed=0 missing_baseline=0`). `ProcessCandidate` carries only `pid`, and `ProcessManager._allowed_processes()` creates a fresh process from that PID and checks current name/user/protected state. A same-PID replacement with the same name/user was terminated by the safe action double. `ProcessManager` has no changed lines in this patch.
Impact: If the original process exits and the OS reuses its PID before confirmation/action, same-user same-name replacement termination is possible. Protected PIDs/names and current-user checks do not identify the originally displayed process.
Verdict: Confirmed P1 PID/action TOCTOU residual under an end-to-end identity contract, but pre-existing PID-only action/model design rather than a patch-introduced two-pass regression. Do not credit scan-time creation checks as action-time identity.
```

```text
Failure mode: Same-user authority is weaker than its broad safety wording.
Evidence: Both `_same_user()` helpers return true for `EVIL\\user` versus `LOCAL\\user`, while protected current PID and protected-name checks remain active.
Impact: Cross-authority same-name processes remain eligible for action. This is a pre-existing normalization policy, not a repair-introduced bypass.
Verdict: Record as a separate pre-existing security residual; do not use it as proof that the current process repair is safe.
```

```text
Failure mode: New optional traversal hooks are incompatible with old private signatures.
Evidence: Default Analyzer calls preserve no-argument scanner methods and the legacy default delegation probe passed. The new optional UI path forwards progress_callback and cancel_event to _download_file_stats and _download_reasons; one-argument replacements raised unexpected-keyword TypeError. The one-argument _file_hash and no-argument process_iter compatibility paths passed.
Impact: Existing private monkeypatches/subclasses can fail before a StorageDialog scan returns.
Verdict: Confirmed conditional P2 compatibility gap if private seams are supported by tests/extensions; not a public/default Analyzer failure or P1 safety issue.
```

**Non-findings and preserved boundaries:**

- Ordinary same-size/same-mtime rewrites are invalidated; the 1,024-entry bound, missing-root pruning, and public same-scanner scan lock are real. The remaining cache issue is the final-marker atomicity window.
- The current Windows wrappers catch DLL-load `OSError` and inner `ctypes.ArgumentError`, assign required `argtypes`, use `c_long` HRESULT returns, and set `CoUninitialize`/`CoTaskMemFree` to `restype=None`. COM success/free/uninitialize and changed-mode probes agree with the Microsoft contracts.
- Current traversal catches initial, iterator, and entry `OSError`, closes iterators, rejects hidden entries, avoids symlink-directory recursion, and sorts candidates and duplicate retention with case-fold-plus-exact-path keys.
- The dashboard final fence is present after resource construction; cancellation set during the final `cpu_count()` was rejected before a snapshot returned.
- Static GPU fallback misses are serialized and error tuples are not cached; macOS recovery, Linux NVIDIA precedence/recovery, and Windows static concurrency passed. NVIDIA remains dynamic rather than a cached result path.
- `run_in_thread()` suppresses tested Tk teardown exceptions, AppWindow drops late queue delivery, and dialog generation/existence checks fence stale UI callbacks. These are delivery fences, not worker joins or action cancellation.
- `ProcessManager` retains current PID/parent/name/user protection checks. `FileManager` rejects symlinks and outside paths, requires a regular file under a directory root, catches `send2trash` `OSError`, and contains no permanent-delete API. The reversible Trash boundary cannot repair a stale cache candidate or PID-only action identity.
- Analyzer no-argument defaults and legacy UI fallback probes passed; compatibility does not prove cancellation completion.

**Verdict:** Unsafe for unconditional Mode A acceptance. The current repair fixes the old metadata-only stale-hit, entry-retention, iterator-error, two-pass process-join, missing-prototype, Tk `RuntimeError`, missing-baseline, GPU-error-cache, and OneDrive-file findings. It still has a confirmed P1 final-marker cache result-integrity blocker and a P2 sentinel-root invariant gap; private traversal-hook compatibility is a conditional P2. PID/action TOCTOU and post-close worker continuation are real but pre-existing/cooperative residuals, not patch-introduced findings. Native/COM/DLL handling, the dashboard final fence, scan-time process identity/protection, GPU recovery, UI delivery fencing, and Trash-only scope have fresh supporting evidence. This section is Validator 3 only and does not act as Agent 5.

### Opposing Agent 4 — External-Research Skeptic
**Fresh portable review status:** I independently reread the current working-tree diff, exact changed tests, `maintenance/models.py`, the complete `PATCH-20260804-001-review.md`, and `docs/bug_hunts/performance_reviews/PERF-20260804-001-opposition.md`. No application code or normal test was edited during this pass. This is a fresh Validator 4 review, not an Agent 5 audit.

**Research question:** After the latest repair, do the Windows Shell/COM calls, every Known Folder/OneDrive/home fallback, `os.scandir` traversal, psutil process identity, cooperative Event/Tk lifecycle, GPU fallback cache, requested Windows output scope, and Trash boundary satisfy their documented contracts and the repository's fail-soft expectations?

**Official sources consulted:**

- Microsoft Learn [`SHGetKnownFolderPath`](https://learn.microsoft.com/en-us/windows/win32/api/shlobj_core/nf-shgetknownfolderpath), [`CoInitializeEx`](https://learn.microsoft.com/en-us/windows/win32/api/combaseapi/nf-combaseapi-coinitializeex), [`CoUninitialize`](https://learn.microsoft.com/en-us/windows/win32/api/combaseapi/nf-combaseapi-couninitialize), and [`CoTaskMemFree`](https://learn.microsoft.com/en-us/windows/win32/api/combaseapi/nf-combaseapi-cotaskmemfree): known-folder output is caller-owned, `CoTaskMemFree` returns `void`, and each `S_OK` or `S_FALSE` initialization must be balanced on the same thread; `RPC_E_CHANGED_MODE` is failure.
- Microsoft Learn [`SHQueryRecycleBinW`](https://learn.microsoft.com/en-us/windows/win32/api/shellapi/nf-shellapi-shqueryrecyclebinw) and [`SHQUERYRBINFO`](https://learn.microsoft.com/en-us/windows/win32/api/shellapi/ns-shellapi-shqueryrbinfo): the call returns an `HRESULT`, accepts a null root on supported Windows versions, requires `cbSize`, and writes 64-bit size/count fields.
- Python 3.12 [`ctypes`](https://docs.python.org/3.12/library/ctypes.html), [`os.scandir`](https://docs.python.org/3.12/library/os.html#os.scandir), [`Path.home`](https://docs.python.org/3.12/library/pathlib.html#pathlib.Path.home), [`threading.Event`](https://docs.python.org/3.12/library/threading.html#event-objects), [`Thread`](https://docs.python.org/3.12/library/threading.html#thread-objects), and the [`Tkinter threading model`](https://docs.python.org/3.12/library/tkinter.html#threading-model): DLL lookup can raise `OSError`; `argtypes` and `restype` define the foreign ABI; directory iteration order is arbitrary and iterator/entry operations can raise `OSError`; `Path` status calls can propagate other filesystem errors; events and daemon-thread shutdown are cooperative; cross-thread Tk calls fail when the event loop is not running.
- Official psutil [`process_iter`](https://psutil.readthedocs.io/en/stable/#psutil.process_iter), [`Process`](https://psutil.readthedocs.io/en/stable/#psutil.Process), and [`Process.as_dict`](https://psutil.readthedocs.io/en/stable/#psutil.Process.as_dict): `attrs` populates `Process.info`, `Process` objects are PID-bound and cached, and creation time is part of the identity boundary; current psutil no longer checks PID reuse in `process_iter`.
- Official [`send2trash` README](https://github.com/arsenetar/send2trash/blob/master/README.rst): paths are moved to the native Trash/Recycle Bin and `TrashPermissionError` and other failures are `OSError` subclasses.

**Fresh commands and probes:**

- `python -m unittest discover -s tests -v` -> 39 passed; `ruff check .` -> passed; changed-file `ruff format --check` -> 8 files formatted; `python -m compileall -q algo.py window.py maintenance tests` -> passed; `git diff --check` -> no whitespace errors apart from Git's existing LF/CRLF warnings.
- `./lr --list` and `./lr impact` were unavailable in this PowerShell workspace. No LR pass is claimed. No type-check pass is claimed in this section.
- Read-only probes used temporary files/directories, mocked native functions, mocked `send2trash`, fake psutil objects, and released all blocking fakes before exit. No real file was moved to Trash and no process action was invoked.

**Boundary findings:**

- **Windows DLL/COM/prototypes:** `maintenance/scanner.py:627-649` and `:941-992` assign the required `argtypes`, use `c_long` HRESULT returns, set `CoUninitialize`/`CoTaskMemFree` to `restype=None`, initialize `cbSize`, copy the known-folder string before cleanup, and balance `S_OK`/`S_FALSE` only. `_WindowsGuid` and `_RecycleBinInfo` measured 16 and 24 bytes. Fake success recorded one `CoTaskMemFree` and one `CoUninitialize`; `RPC_E_CHANGED_MODE` made no Shell call and no uninitialize; DLL lookup, `ctypes.ArgumentError`, failed HRESULT, negative-size, and live read-only Windows calls returned the expected `None`/`0`/nonnegative results. The common native path is supported.
- **Native ownership edge:** A failure fake that supplied a non-null pointer to an empty string had a nonzero pointer address but recorded zero `CoTaskMemFree` calls, because cleanup tests `if path_pointer.value` rather than pointer presence. Microsoft requires the caller to free the returned allocation whether `SHGetKnownFolderPath` succeeds or fails. This is a conditional P2 ownership gap for a malformed/API-variant failure, not a reproduced normal Windows return, but it prevents a complete native failure sign-off.
- **Known Folder and fallback matrix:** Fresh normal cases passed for an existing Known Folder directory, Known Folder file -> OneDrive directory, Known Folder file/OneDrive file -> home Downloads directory, missing Known Folder -> OneDrive directory, and regular home Downloads -> `Downloads.__unavailable__` on Windows and non-Windows. A missing home Downloads returns the non-existing `home/Downloads` path, which the scanner and action layer handle as unavailable. A missing or non-directory Known Folder is correctly not selected. If `Downloads.__unavailable__` already exists as a regular file, `_safe_downloads_fallback()` returns that file; `FileManager` later rejects it and makes no `send2trash` call, so this is a P2 default-root invariant gap rather than a Trash escape. The documented `Path.is_dir()`/`Path.exists()` error behavior is not fail-soft here: injected `OSError` from the Known Folder directory check and home fallback existence check escaped instead of trying the next fallback. OneDrive uses the same unguarded directory predicate. This is a conditional P2 fallback error gap.
- **`os.scandir`, ordering, and errors:** `maintenance/scanner.py:414-468` catches initial `scandir` failure, iterator `__next__` failure, and per-entry type/stat errors, and closes the iterator. Fresh open-error, iterator-error, and entry-error probes returned `{}` and closed the fake iterator. Nested output was ordered `a\\file.bin`, `b\\file.bin` despite filesystem traversal order; final candidates sort by size, case-folded path, and exact path at `:405-412`, and duplicate retention uses the same deterministic path key at `:568-573`. No current traversal/order regression was reproduced.
- **Hash cache identity:** The ordinary same-size/same-timestamp rewrite test, missing-root pruning (`2` entries -> `0`), and bounded-cache tests pass. However, a fresh hit-path probe mutated the file immediately after the second content marker and returned `CACHE_FINAL_MARKER_RACE False marker_calls 2`: the returned digest did not match the bytes present after return. The final marker/stat check is therefore not atomic with returning the cached digest. A changed Downloads file can be classified as a duplicate and present the wrong in-root file to the reversible Trash action. This remains a confirmed P1 result-integrity blocker under the no-behaviour-change contract.
- **psutil snapshot identity:** The repaired scanner invokes one `process_iter(attrs=["pid", "create_time"])`, records the baseline creation time, and rejects changed or missing current creation time. Fresh fakes returned `PROCESS_STABLE 1 1 4242`, `PROCESS_CHANGED_CREATION 0 1`, and `PROCESS_FINAL_CANCEL ScanCancelled`; the one-iterator scan-time identity repair is supported. `ProcessCandidate` still carries only `pid`, and `ProcessManager._allowed_processes()` creates a new PID-bound `Process` and checks current name/user/protection only. A safe action double returned `PID_ACTION_HAS_NO_SNAPSHOT_TOKEN True True`, demonstrating the pre-existing action-time PID replacement residual. Do not credit scan-time creation checks as end-to-end action identity.
- **Event and Tk behavior:** The dashboard's final cancellation check at `scanner.py:240` is present: a fake final `cpu_count()` set the event and produced `DASHBOARD_FINAL_FENCE ScanCancelled cpu_count_calls 2`. `run_in_thread()` now suppresses both `RuntimeError` and `tk.TclError` from teardown delivery; the fake widget probe produced `TK_RUNTIME_DELIVERY True 0`. These are delivery fences, not interruption guarantees. A close probe showed `APP_CLOSE_COOPERATIVE True False` until the blocking worker was explicitly released, after which it finished. This matches the official cooperative `Event`/daemon-thread semantics, but dialog action workers have no cancellation event and workers are not retained or joined. Treat worker quiescence and post-close action continuation as P2 lifecycle residuals unless the product contract is explicitly UI fencing only.
- **GPU fallback:** Fresh deterministic probes showed transient static fallback error -> successful recovery, NVIDIA dynamic precedence without a static call, and two concurrent static callers sharing one loader invocation. `_cached_static_gpu_details()` caches only successful static details, while NVIDIA remains dynamic. No current recovery, precedence, or static-cache synchronization defect was reproduced.
- **Requested Windows output scope:** The prior PERF artifact explicitly permits Known Folder resolution and Recycle Bin sizing. The current successful Windows output changes observed here are limited to the Shell-selected Downloads root and the nonnegative Recycle Bin byte count; no broader Windows output change was established. The fallback/root invariant above remains a correctness issue within that approved root-resolution surface.
- **`send2trash` boundary:** `maintenance/actions.py:177-209` resolves strictly, rejects selected symlinks, requires a directory root and regular in-root file, passes a string path, catches `OSError`, and contains no permanent-delete API. Mocked probes made exactly one call for a valid in-root file, none for an outside file or file-valued root, and converted a mocked `OSError` into an action error. Symlink execution was unavailable because this host denied symlink creation; the explicit guard was read. A synthetic `allowed_root.is_dir()` `OSError` escaped before the result object, which is a conditional filesystem-validation fail-soft gap distinct from send2trash's documented error handling.
- **Optional private hooks:** Public/default Analyzer calls omit optional keywords and the legacy UI fallback for public analyzer methods remains callable. The optional UI scan path still forwards `progress_callback` and `cancel_event` to old one-argument `_download_file_stats` and `_download_reasons` hooks; fresh probes produced unexpected-keyword `TypeError` for both. This is a conditional P2 private compatibility gap, not a default-host Analyzer failure.

**Verdict:** Unsafe for unconditional Mode A acceptance under PERF's `Allowed behaviour changes: none`. The native common path, normal Known Folder/OneDrive/home selection, scandir error handling and deterministic ordering, scan-time psutil identity, final dashboard cancellation fence, Tk delivery suppression, GPU recovery/precedence, approved Windows output scope, and reversible Trash boundary are supported. Acceptance remains blocked by the reproduced P1 final-marker cache race. Additional P2 gaps are the sentinel-file fallback collision, unhandled fallback/root filesystem predicate errors, private optional-hook compatibility, and the conditional empty-pointer native ownership edge. PID-only action identity and worker/action continuation after close are real residuals but pre-existing/cooperative boundaries rather than proof of a new scan join regression. This section is Validator 4 only and does not act as Agent 5.

## Agent 5 evidence audit

**Threshold declared in header:** full A4

**Agent 5 required by this threshold:** yes

**Reasoning:** This is a high-risk, contradictory Mode A repair spanning content identity, filesystem traversal, process snapshots, cancellation, Tk teardown, cleanup scope, GPU fallback state, and Windows ctypes/COM boundaries. All four fresh validator sections were complete before this independent portable audit. Passing unit and lint gates cannot establish these cross-boundary contracts.

**Agent 5 run for this review:** yes

**Reason Agent 5 was/was-not run:** This portable independent audit read the complete current patch-review artifact, the current working-tree diff, the original task summary, the complete prior `PERF-20260804-001` opposition, exact changed code/tests/callers, all fresh validator commands and measurements, and the cited official documentation. It ran additional read-only counter-tests. No application code or normal test was edited.

**Evidence completeness:** Sufficient for a reject/transition decision, not for acceptance. My current checks report `python -m unittest discover -s tests -v` -> 38 passed in 0.692s, `ruff check .` -> passed, changed-file `ruff format --check` -> 8 files formatted, compileall -> passed, and `git diff --check` -> no whitespace errors apart from existing LF/CRLF warnings. `./lr --list` is unavailable in this PowerShell workspace and is not a pass. The fresh type-check reports are non-clean for the recorded missing stubs/configuration/Tk/test diagnostics. The fresh performance evidence is enough to narrow the claim, not to prove an end-to-end Windows gain.

**Independent counter-tests:**

- `CACHE_ATOMIC hit False 2` and `CACHE_ATOMIC miss False 2`: mutating the file immediately after the second marker caused both cache-hit and cache-miss paths in `maintenance/scanner.py:720-770` to return a SHA-256 digest that did not match the file after return. The ordinary same-size/same-mtime rewrite test still passes; this is the remaining final-check race, not the old metadata-only invalidation case.
- `PRIVATE_HOOK file_stats ... unexpected keyword argument 'progress_callback'` and the same result for `_download_reasons`: public no-option `scan_downloads()` still forwards new keywords to old one-argument private hooks. The existing one-argument `_file_hash` compatibility test passes, but these traversal/reason hooks are not compatible. `_is_download_file()` remains defined but is no longer used by the replacement walk.
- `DEFAULT_ROOT True True () []`: a regular final Windows home `Downloads` fallback became both the resolver result and `FileManager.allowed_root`, but `move_to_trash()` made no mocked `send2trash` call. This disproves a Trash escape, while confirming the resolver can violate its directory-only invariant and produce an unusable scan/action root.
- `DLL_FAILURE 0 None` and `COM_CHANGED_MODE None 0 0`: outer DLL-load `OSError` returns fail-soft values, and `RPC_E_CHANGED_MODE` returns no path without calling `CoUninitialize` or the Shell function. A blocking fake process iterator remained alive after cancellation (`PROCESS_BLOCKING True`) and then ended with `ScanCancelled`; a normal action worker remained unfinished after its event was set (`WORKER_CANCEL False`) until its blocking operation was released.
- Creation-time probes returned `stable 1`, `changed 0`, and `missing 0`. The repaired scan-time identity guard works for those cases. It does not bind `ProcessCandidate` to action-time identity.

**Official semantics and repo implications:** Microsoft documents that `SHGetKnownFolderPath` returns caller-owned memory released with `CoTaskMemFree`, that `CoInitializeEx` returns `S_OK`/`S_FALSE` for successful initialization and `RPC_E_CHANGED_MODE` for failure, and that `SHQueryRecycleBinW` takes an initialized `cbSize` and returns an `HRESULT`: [SHGetKnownFolderPath](https://learn.microsoft.com/en-us/windows/win32/api/shlobj_core/nf-shlobj_core-shgetknownfolderpath), [CoInitializeEx](https://learn.microsoft.com/en-us/windows/win32/api/combaseapi/nf-combaseapi-coinitializeex), [CoTaskMemFree](https://learn.microsoft.com/en-us/windows/win32/api/combaseapi/nf-combaseapi-cotaskmemfree), and [SHQueryRecycleBinW](https://learn.microsoft.com/en-us/windows/win32/api/shellapi/nf-shellapi-shqueryrecyclebinw). Python documents explicit `ctypes` prototypes and default C-int returns, arbitrary `os.scandir` order and `OSError` iteration failures, cooperative `Event` signalling, daemon-thread shutdown, and Tk calls failing when the event loop is not running: [ctypes](https://docs.python.org/3.12/library/ctypes.html), [os.scandir](https://docs.python.org/3.12/library/os.html#os.scandir), [threading](https://docs.python.org/3.12/library/threading.html#event-objects), [Thread objects](https://docs.python.org/3.12/library/threading.html#thread-objects), and [Tkinter threading](https://docs.python.org/3.12/library/tkinter.html#threading-model). psutil documents PID-bound `Process` objects, PID-reuse limitations, and `process_iter(attrs=...)`: [psutil process_iter](https://psutil.readthedocs.io/en/stable/#psutil.process_iter). `send2trash` documents native reversible Trash/Recycle Bin moves and `OSError` failures: [README](https://github.com/arsenetar/send2trash/blob/master/README.rst).

**Correctness-before-speed check:** Failed for unconditional Mode A acceptance. The repair is supported for ordinary full-content invalidation, the 1,024-entry cap, public same-scanner Downloads serialization, iterator/per-entry fail-soft traversal, total candidate and duplicate ordering keys, one process iterator, changed/missing creation-time rejection, Analyzer no-argument defaults, legacy UI analyzer fallback, explicit native prototypes, inner conversion-error fallbacks, COM success/free/uninitialize sequencing, NVIDIA precedence, static GPU recovery, Tk `RuntimeError`/`TclError` delivery suppression, stale UI delivery fencing, protected-process checks, and the reversible `send2trash` boundary. The current cache atomicity race, final fallback root invariant, private traversal-hook compatibility, and worker/action lifecycle evidence prevent acceptance.

**Contract preservation check:** Public result dataclass shapes and no-option Analyzer calls remain intact. The UI compatibility fallbacks passed fresh probes. The replacement traversal catches initial, iterator, and per-entry `OSError`, and final candidate sorting plus duplicate retention use `(casefolded path, exact path)` keys; no current deterministic-order regression was reproduced. However, the private `_download_file_stats` and `_download_reasons` seams fail under their prior one-argument shapes. A missing root returns early before cache pruning, retaining old entries until a later existing-root scan. The cache is entry-bounded and scanner-local, not byte/age-bounded, and direct private concurrent misses are not coalesced.

`ProcessCandidate` still contains only `pid`. `ProcessManager._allowed_processes()` reopens that PID and checks current name/user/protected state, so a same-PID replacement with the same name and normalized user can pass action-time checks. This is a pre-existing P1 identity residual rather than a new two-pass regression, but the patch must not claim end-to-end process snapshot identity without carrying/revalidating a creation token or explicitly narrowing that contract. The current scan repair is scan-time only.

**Security/privacy/fail-closed check:** No cross-user or persisted cache leak was reproduced. `FileManager` rejects a file-valued root, selected symlinks, outside paths, and non-files, catches `send2trash` `OSError`, and introduces no permanent-delete API. The Trash boundary is preserved. The cache race can still display/classify the wrong in-root duplicate and send that wrong valid path to reversible Trash; it does not bypass the root or permanent-delete boundary. The default resolver's final home fallback is still not directory-validated on Windows or non-Windows, but the action layer fails closed, making this a P2 default-root correctness gap rather than the stale P1 Trash-escape claim in the superseded audit.


**Windows native check:** No current native correctness blocker was reproduced. Both outer DLL lookups catch `OSError`; required `argtypes`, HRESULT restypes, `restype = None` cleanup functions, COM balancing, `CoTaskMemFree` ownership, HRESULT checks, and negative-size handling are present. Fresh fake and live read-only probes returned the documented fail-soft values and a nonnegative Recycle Bin size. The old claims that DLL-load failures, missing prototypes, inner `ArgumentError`, or changed-mode cleanup currently escape are disproven. Remaining evidence is limited to fake failure injection plus one successful host smoke, so this boundary is supported but not a substitute for broader Windows variant testing.

**GPU and output-scope check:** Static macOS/Windows/Linux fallback errors are not cached, successful static fallback details are serialized and cached per scanner, and NVIDIA remains dynamic and is queried again rather than incorrectly cached. Fresh recovery, precedence, and concurrency probes support this behavior. The requested Windows Known Folder resolution and Recycle Bin sizing are explicitly permitted behavior changes in the task/opposition; their successful path/root/output changes are therefore not a no-behavior-change blocker. No broader Windows output change was established. Performance claims must still distinguish static fallback reuse from NVIDIA or dashboard-result caching.

**Failure timing and worker lifecycle check:** `Event` cancellation is cooperative, not interruption or termination. Blocking process iteration, filesystem/native/GPU calls, and action work can continue after cancellation or close. Dashboard has checkpoints but no final checkpoint after resource construction; dialogs fence late UI delivery and cancel scan events, while action workers receive no cancellation event. `AppWindow._run_in_background()` and `run_in_thread()` create daemon workers without retaining handles or joining them; close can destroy Tk while workers remain alive and queue late payloads. This is a confirmed P2 lifecycle/contract gap. It is acceptable only if the product contract is explicitly UI-fencing rather than worker quiescence; the current patch-review scope does not document that narrowing.

**Performance measurement check:** Honest but narrow. The live Downloads scan timed out under the fresh 20-second containment run and remained unresolved under the prior 120-second benchmark. Process comparisons changed candidate counts and `scan_processes()` has no result cache. Dashboard repeats still perform live system queries; a warm duplicate scan still reads full-content markers for candidates and primarily skips SHA-256 work; static fallback GPU reuse is supported, while successful NVIDIA results are not cached. The defensible claim is limited to scanner-local static fallback reuse and digest-work avoidance on stable repeated duplicate scans. There is no evidence for a general cold-start, process-speed, dashboard-result-cache, live-tree completion, or end-to-end Windows responsiveness gain.

**Missing evidence:**

- An atomic content-identity strategy and regression proof covering mutation after the last marker, path replacement, and concurrent cache hit/miss behavior.
- Compatibility tests or an explicit disposition for the old one-argument `_download_file_stats` and `_download_reasons` hooks, plus a documented private-hook policy.
- Directory-only tests for the final home fallback on both platform branches; the current action fail-closed test is not resolver proof.
- Action-time process creation-token/PID-reuse evidence, or an explicit narrowing that process identity is scan-time only.
- Cancellation tests at blocking iterator/read/native boundaries, the final dashboard construction boundary, action cancellation, and worker join/quiescence; alternatively, an explicit UI-only-close contract.
- Broader native Windows failure/variant coverage beyond fakes and the successful host smoke, and a bounded representative Downloads benchmark with matched before/after process workloads.
- LR/drift output remains unavailable; type checks remain non-clean for the recorded repository/stub/configuration reasons.

**Blocking issues:**

- **P1 result integrity:** `_cached_file_hash()` can return a digest after the last content marker no longer describes the file. A wrong duplicate can therefore be presented and selected for reversible Trash.
- **P2 default-root invariant:** the final home fallback can be a regular file. The action layer fails closed, but the scanner/root contract is still invalid and silently unusable.
- **P2 private compatibility:** public no-option scans invoke old one-argument traversal/reason hooks with unexpected keywords.
- **P1 contract residual, pre-existing:** PID-only action revalidation does not establish identity of the displayed process. Do not credit the scan-time repair as end-to-end action safety.
- **P2 lifecycle:** cancellation/close does not guarantee worker completion; confirmed process/file actions may continue after a dialog closes.

**Non-blocking findings:** Native DLL/COM failure handling, GPU recovery/precedence, deterministic Downloads ordering, UI delivery fencing, protected-process checks, and the Trash-only API boundary are supported by current code and fresh probes. The 1,024-entry cache bound is real but only an entry bound; missing-root pruning and direct private concurrent cache transactions remain residual policy/performance concerns. The domain-stripping same-user rule is pre-existing and separate from this patch. The performance claim must remain scanner-local and warm-path-specific.

**Mode transition check:** Keep the repair in Mode A. It is not safe to accept unconditionally, and it is not eligible to return to Mode C. The current evidence does not require a Mode B transition for the pre-existing PID/user residual. Repair or explicitly narrow the cache, root, private-hook, process-action, and lifecycle contracts, then rerun correctness checks and fresh full-A4 validation before any renewed performance comparison.

**Agent 5 final verdict:** **Transition required.** Reject unconditional Mode A acceptance and do not return this patch to Mode C. The patch repairs many prior findings, but the reproduced cache race is a correctness and Trash-target blocker; root directory invariant and private hook compatibility remain concrete contract failures; process action identity and worker quiescence remain unproven/residual. Only the narrow scanner-local warm claims are currently defensible.

## Main auditor rebuttal

The completed A4 evidence required a `C -> A` repair and did not accept the earlier tree. Subsequent local repairs addressed the reported native DLL/COM, fallback-root, private-hook, GPU-recovery, ordering, cancellation-checkpoint, and sentinel-collision findings. The current local gates pass at 41 tests, Ruff, scoped formatting, compileall, and diff checks. Per the user's instruction, no further validator/Agent 5 run was started after those last edits, so the current tree is not A4-cleared. The remaining documented risk is the inherent cache-hit/filesystem TOCTOU window, cooperative daemon-worker shutdown, and pre-existing PID-to-action identity boundary.

## Final patch decision

needs more evidence

## Remaining risk

The live Downloads path still lacks a completed representative before/after benchmark. Warm scanner-local cache effects are narrower than a general Windows responsiveness claim, and the final small code edits lack a fresh independent review.
