# PATCH-20260906-003 - discovered-peer dashboard status

## Mode and scope

BugGuard mode: Mode A - code-edit validation
Risk level: medium
Validator threshold: selective
Reason: Present untrusted local-network peer presence without changing trust,
selection, transport, or action authority.

## Change

The dashboard header now renders a compact, read-only `Description.TLabel` only
when discovery candidates exist. It lists the number of untrusted peers and up
continues to exclude all discovered/untrusted candidates.

## Evidence

- Focused UI/node/live-Tk suite: 97 passed.
- A real Tk render mapped `Discovered 1 untrusted peer: macbook` after a
  synthetic candidate event.
- Full suite: 767 passed.
- `ruff check .`, `ruff format --check .`, `pyright .`, and
  `mypy --ignore-missing-imports .` passed.
- `install/verify.sh 1.2.4.0` passed; wheel contents and SHA256SUMS match.

## Remaining risk

The label is presentation-only; real cross-device mDNS remains to be verified
on the user's macOS and Linux network after installing the wheel. Independent
opposition is unavailable in this environment and is not represented as
completed.
