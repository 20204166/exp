# PATCH-20260911-001 - Mode A patch review

## Artifact ownership

The Mode A patch-review artifact is the source of truth for adversarial review.
The main auditor owns this scaffold, patch summary, evidence index, and final synthesis. Each selected reviewer and Agent 5 must write only its own section directly into this file.

## Mode and risk

BugGuard mode: Mode A - code-edit validation
Risk level: high
Validator threshold: full A4
Reason: coordinator roles, authenticated fencing, persistence, bounded storage, failover, and UI controls cross multiple runtime and security boundaries.

## Patch summary

Complete the coordinator/worker role and failover plan without adding a second scheduler or executor. The patch adds pure role and fencing decisions, persisted role state and invites, authenticated role operations, bounded Coordinator/standby storage, heartbeat and failover lifecycle integration, UI controls, diagnostics, migration coverage, and regression tests.

## Original bug / task evidence

- User requested full implementation of `docs/plans/2026-09-11-coordinator-worker-roles.md`.
- The plan requires one active Coordinator, at most one Subcoordinator, epoch fencing, bounded storage, failover after 120 seconds, return fencing, role controls, diagnostics, and adversarial validation.
- The current repository architecture requires reuse of `AppCoordinator`, `ComponentRefreshScheduler`, existing peer lifecycle, target-side authorization, and atomic cluster persistence.

## Diff summary

See the current working-tree diff and the plan-linked files under `maintenance/`, `maintenance/components/`, `maintenance/ui/`, `tests/`, `docs/`, and `dist/`.

## Tests and checks run

- Focused lifecycle, storage, role, and remote suites: passed.
- Full repository suite before the final data-gap addition: 1226 tests passed.
- `compileall`: passed.
- `git diff --check`: passed.
- Full suite and release rebuild will be rerun after the opposition cycle.

## Static/type/format checks

- `ruff`, `pyright`, and `mypy` are not installed in the environment and are therefore not treated as passing.

## Selected validators

- Opposing Agents 1-4: required, independent, full A4.
- Agent 5 evidence audit: required after all four opposing sections are complete.

## Validator findings

### Opposing Agent 1 - Patch/Reproduction Skeptic

**Review date: 2026-09-11. Verdict: do not accept as-is; one confirmed P1 UI/lifecycle defect remains. The latest dashboard footer move has no reproduced defect.** The focused dashboard/Cluster/window suite passed 176 tests and the related UI suite passed 96 tests.

**P1: Cluster-page Revoke persists role revocation but does not perform the required trust/provider cleanup.** The plan requires Revoke to invalidate the provider, remove cluster visibility, and require a new invite (`docs/plans/2026-09-11-coordinator-worker-roles.md:425-430`). The corrected composition now wires `ClusterPageCallbacks.on_revoke` to `_revoke_node` (`maintenance/ui/window_pages.py:146-154`), and that handler persists `RoleState.revoke()` through `_save_role_state` (`maintenance/ui/window_node_actions.py:159-169,88-116`). It does not invalidate the registered provider, remove `trusted_nodes` or `peer_grants`, cancel active node resources, or remove the node from the registry. Those effects remain only in `_revoke_trusted_node` (`maintenance/ui/window_node_actions.py:454-510`). Read-only source probes returned `cluster_revoke_callback_is_role_revoke=True`, `role_revoke_calls_role_state=True`, `role_revoke_cleans_provider=False`, and `trust_revoke_cleans_provider=True`. Thus the callback correction fixes the earlier misrouting, but a Cluster-page Revoke can still leave the node trusted, connected, and visible after its role is marked revoked. Severity P1 because the authority-bearing control does not enforce the documented role/trust/provider lifecycle invariant.

**Dashboard footer move: no confirmed bug.** `dashboard_meta_frame` is created and packed after `cards_container` (`maintenance/ui/dashboard_page.py:141-150,182-200`), with refreshed time on the left and target status on the right. The runtime path still updates both labels, including the no-snapshot reset (`maintenance/ui/window_node_runtime.py:247-277`). The direct source probe confirmed `dashboard_footer_after_cards=True`; existing focused tests passed. No evidence shows a lost update or callback regression.

**Fixed or unconfirmed counter-checks:** The corrected callback wiring, non-finite invite filtering, duplicate-Coordinator parsing, epoch validation, promotion storage/import, failed invite-save rollback, target-first invite validation, revoked reassignment, `resume_worker` validation, upload callers, and role projection remain present. Exact lease expiry remains a contract-clarification gap (`can_promote()` returns true at equality), not a confirmed bug. The split `import_batch()` prior-read/gap-insert/append sequence remains an unforced concurrency concern; no duplicate committed row was reproduced. The storage cap was not reclassified here: its logical-versus-physical meaning remains conditional on the contract.

**Validation:** `python3 -m unittest tests.test_dashboard_ui tests.test_cluster_page tests.test_window_nodes tests.test_window -q` -> `Ran 176 tests ... OK`; `python3 -m unittest tests.test_nodes_connections_page tests.test_ui_primitives tests.test_live_tk_resize -q` -> `Ran 96 tests ... OK`. Read-only probes confirmed the revoke-path split and footer ordering. No application code or tests were edited; only this reviewer section was replaced.

**Limitations:** The revoke check used source inspection and synthetic local objects; it did not run a live Tk click, inspect a real provider exchange after revocation, or run a two-installation/TLS scenario. The footer check did not obtain a live display, so resize/render geometry was not visually verified; no direct test asserts the new metadata-frame hierarchy. No restart/power-loss recovery, native macOS/Windows run, OS-level disk-full test, or forced concurrent gap race was run. `ruff`, `ruff format`, `pyright`, `mypy`, and `./lr` are unavailable locally and are not passing evidence. No external services, credentials, process termination, or destructive actions were used. This is the independent portable fallback Agent 1 pass, not a registered-agent result.

### Opposing Agent 2 - Repo-Truth Skeptic

**Review date: 2026-09-11. Verdict: do not accept as-is; one P1 lifecycle residual and one conditional P2/P3 storage-contract residual remain. The current tree does not support the earlier callback-miswire, malformed-invite, duplicate-Coordinator, promotion-storage, or failed-save invite findings.**

**P1 residual: Cluster-page Revoke persists role revocation but does not perform the plan-required trust/provider cleanup.** The current composition passes `controller._revoke_node` to `ClusterPageCallbacks.on_revoke` (`maintenance/ui/window_pages.py:140-157`), and that path calls `RoleState.revoke()` through `_save_role_state()` (`maintenance/ui/window_node_actions.py:88-116,159-169`). This fixes the earlier claim that the page bypassed persisted role revocation. However, `_revoke_node()` only changes the role assignment and refreshes projections. The separate `_revoke_trusted_node()` path is the one that removes `trusted_nodes` and `peer_grants`, cancels node work, invalidates the provider, clears resources, and removes registry trust (`maintenance/ui/window_node_actions.py:454-510`). The plan requires Revoke to invalidate the provider, remove cluster visibility, and require a new invite (`docs/plans/2026-09-11-coordinator-worker-roles.md:425-430`). Severity P1 because the visible control can leave a revoked node trusted, connected, and visible while its persisted role is revoked.

**P2/P3 conditional residual: the configured storage cap is logical payload accounting, not a physical SQLite/disk bound.** `CoordinatorTimeline.append()` gates on caller-provided `encoded_size`, while `status().bytes_used` sums that field (`maintenance/components/cluster_storage.py:208-264,336-350`). It does not measure SQLite pages, the database file, journal/WAL sidecars, or filesystem allocation. A fresh temporary probe with `max_bytes=1` and `encoded_size=0` returned `zero_size_append=True`, `logical_bytes=0`, and `physical_bytes=28672`. This is P2 only if “bounded storage” promises a physical hard budget; otherwise it is P3 or a contract/documentation clarification. No ordinary declared-payload bypass or disk exhaustion was reproduced.

**Retained uncertainty only:** The storage-cap probe establishes that `max_bytes` is logical payload accounting rather than a physical SQLite/disk bound; see the conditional residual immediately above. No ordinary declared payload bypass or disk exhaustion was reproduced.

**Dashboard footer move: no confirmed bug.** `dashboard_meta_frame` is created and packed after `cards_container` (`maintenance/ui/dashboard_page.py:141-150,182-200`). The selected-node render path updates both the target-status label and refreshed label, including the no-snapshot reset (`maintenance/ui/window_node_runtime.py:241-277`). A fresh source probe confirmed `dashboard_footer_after_cards=True`; no lost update or callback regression was found. There is no direct test asserting the new metadata-frame hierarchy, so this is source/test evidence rather than live visual evidence.

**Fixed or disproven by current-tree evidence:** `_parse_invites()` filters non-finite expiry construction failures (`maintenance/cluster.py:857-874`); `_parse_roles()` skips a second active Coordinator (`maintenance/cluster.py:801-836`); `CoordinatorEpoch` rejects non-finite timestamps (`maintenance/components/cluster_roles.py:78-87`); promotion storage/import, failed invite-save rollback, revoked reassignment, remote fencing/authorization, `resume_worker` validation, upload callers, and Coordinator-only projection remain present. `import_batch()` still separates prior-read, gap insert, and append across connections, but no duplicate/stale-gap race was forced; exact lease-expiry equality remains a contract clarification, not a confirmed bug.



**Evidence:** From `/home/btn17/Downloads/exp`, `python3 -m unittest tests.test_cluster_roles tests.test_cluster_roles_persistence tests.test_cluster_storage tests.test_cluster_failover tests.test_cluster_adversarial tests.test_cluster_page tests.test_nodes_connections_page tests.test_dashboard_ui -q` -> `Ran 83 tests ... OK`; `python3 -m unittest tests.test_cluster -q` -> `Ran 35 tests ... OK`; `python3 -m compileall -q maintenance tests && git diff --check` -> exit 0. Fresh read-only probes returned `cluster_revoke_wires_controller_method=True`, `role_revoke_calls_role_state=True`, `role_revoke_cleans_provider=False`, `trust_revoke_cleans_provider=True`, `dashboard_footer_after_cards=True`, `zero_size_append=True`, `logical_bytes=0`, and `physical_bytes=28672`. No application code or normal tests were edited; only this Agent 2 section was replaced.

**Limitations and reviewer status:** No live Tk click/display render, two-installation/TLS exchange, restart/power-loss recovery, native macOS/Windows run, OS-level disk-full test, wheel verification, or forced concurrent gap race was run. `ruff check .`, `ruff format --check .`, `pyright`, `mypy --ignore-missing-imports .`, and `./lr` are unavailable locally and are not passing evidence. Probes used source inspection, synthetic objects, and temporary local storage only; no external services, credentials, process termination, or destructive actions were used. This is the independent portable fallback Agent 2 review, not a registered-agent result.

### Opposing Agent 3 - Architecture/Security Skeptic

**Review date: 2026-09-11. Verdict: do not accept as-is; one confirmed P1 lifecycle residual and one conditional P2/P3 storage-contract residual remain.** This is an independent portable fallback Agent 3 review against the exact current worktree, not a registered-agent result. The architecture has meaningful controls: authenticated envelopes select the grant by caller identity, target identity is checked, role operations require cluster/epoch/fencing fields, and pure role transitions reject inactive or revoked actors.

**P1 confirmed: Cluster-page `Revoke` does not complete the documented revocation lifecycle.** The plan requires Revoke to invalidate the provider, remove cluster visibility, and require a new invite (`docs/plans/2026-09-11-coordinator-worker-roles.md:425-430`). Current composition correctly wires the Cluster page to `_revoke_node` (`maintenance/ui/window_pages.py:140-157`), and that handler calls `RoleState.revoke()` and persists `role_assignments` (`maintenance/ui/window_node_actions.py:159-169,88-116`). However, it does not remove `trusted_nodes` or `peer_grants`, cancel active node resources, invalidate the provider, or remove the node from the registry. Those cleanup operations remain in the separate `_revoke_trusted_node()` path (`maintenance/ui/window_node_actions.py:454-510`). The source probe returned `cluster_revoke_callback_is_role_revoke=True`, `role_revoke_calls_role_state=True`, and `trust_revoke_invalidates_provider=True`; the paths are separate. A visible Cluster-page revoke can therefore leave the revoked node trusted, connected, and visible after its persisted role is marked revoked. Severity P1 because an authority-bearing security control crosses the role, trust, provider, and resource lifecycle boundaries without enforcing the documented invariant.

**No additional caller/target/cluster authorization bypass was confirmed.** `RemoteService.handle()` resolves grant credentials from the authenticated caller, rejects an unexpected caller and wrong target, then applies role fencing before dispatch (`maintenance/remote.py:709-750`). `_verify_role_fence()` checks cluster identity, epoch, and fencing token (`maintenance/remote.py:901-910`). `RoleState.assign/pause/resume/revoke()` require an active Coordinator and reject reassignment of revoked nodes (`maintenance/components/cluster_roles.py:147-218`). The focused remote, adversarial, role, and failover suites passed; no cross-node cache/resource mix-up or privilege escalation was reproduced.

**Dashboard footer/update path: no confirmed defect.** The metadata frame is packed after the scrollable cards (`maintenance/ui/dashboard_page.py:141-150,182-200`). Selected-node rendering updates target status, refreshed time, scan time, cards, and health, and resets the labels on a missing snapshot (`maintenance/ui/window_node_runtime.py:241-277`). This is source and fake-widget evidence, not live visual geometry evidence; no direct footer-hierarchy assertion exists.

**Residual uncertainties, not confirmed bugs:** `can_promote()` permits exact lease expiry because it rejects only `now < lease_expires_at` (`maintenance/components/cluster_roles.py:242-258`); the plan does not define endpoint inclusivity. `import_batch()` separates prior-read, gap insertion, and append across connections (`maintenance/components/cluster_storage.py:266-311`); no deterministic duplicate/stale-gap race was forced. Non-finite invite expiry and duplicate active-Coordinator persistence claims are disproven by the current parser/invariant paths (`maintenance/cluster.py:801-836,857-874`; `maintenance/components/cluster_roles.py:78-87`). Promotion storage/import and failed invite-save rollback remain present (`maintenance/ui/window_discovery.py:392-417`; `maintenance/ui/window_discovery.py:161-193`).

**P2/P3 conditional residual: `max_bytes` is logical payload accounting, not a physical SQLite/disk bound.** `append()` gates on caller-provided `encoded_size`, while `status().bytes_used` sums that field (`maintenance/components/cluster_storage.py:208-264,336-350`). The implementation does not account for SQLite pages, indexes, journal/WAL sidecars, or filesystem allocation. This is P2 only if “bounded storage” promises a physical hard budget; otherwise it is P3 or a contract/documentation clarification. No ordinary declared-payload bypass or disk-exhaustion condition was reproduced.

**Evidence and limitations:** `python3 -m unittest tests.test_cluster_roles tests.test_cluster_roles_persistence tests.test_cluster_storage tests.test_cluster_failover tests.test_cluster_adversarial tests.test_remote_contract tests.test_remote_security tests.test_peer_connection tests.test_nodes_connections_page tests.test_cluster_page tests.test_dashboard_ui tests.test_window_nodes -q` passed 200 tests; `python3 -m unittest discover -s tests -q` passed 1228 tests; `python3 -m compileall -q maintenance tests && git diff --check` passed. Read-only source probes returned `cluster_callback=True`, `role_revoke_has_cleanup=False`, `trust_revoke_cleanup_tokens=True`, `role_caps_permissions_match=True`, and `exact_expiry_can_promote=True`. No application code or normal tests were edited. `ruff check .`, `ruff format --check .`, `pyright`, `mypy --ignore-missing-imports .`, and `./lr` are unavailable and are not passing evidence. No live Tk click/display render, two-installation/TLS exchange, restart/power-loss recovery, native-platform run, OS-level disk exhaustion, or forced gap race was run; no external services, credentials, process termination, or destructive actions were used. The registered BugGuard reviewer topology is unavailable; this is the requested independent portable fallback Agent 3 review, not a registered-agent result.


### Opposing Agent 4 - External-Research Skeptic

**Review date: 2026-09-11. Verdict: do not accept as-is.** Official Python and SQLite documentation confirms the language/database semantics below, but cannot establish this application's role, revocation, failover, or UI contract. The exact current tree supports one P1 role-control residual and one conditional P2/P3 storage-contract residual; earlier malformed-persistence claims remain disproven.

**Current-tree reconciliation:** Promotion creates/imports Coordinator storage and updates the listener fence (`maintenance/ui/window_discovery.py:392-417`); failed invite persistence restores the consumed invite (`maintenance/ui/window_discovery.py:161-180`); `CoordinatorEpoch` rejects non-finite timestamps (`maintenance/components/cluster_roles.py:78-87`); and HMAC checks use `hmac.compare_digest()` (`maintenance/remote.py:421-429,501,907,1045`). Fresh source probes found non-finite invite construction is caught and duplicate active Coordinator assignments are skipped. These are not residual bugs.

**P1 residual: Cluster-page Revoke does not complete the documented revocation lifecycle.** The current composition wires `ClusterPageCallbacks.on_revoke` to `controller._revoke_node` (`maintenance/ui/window_pages.py:140-157`). That path persists `RoleState.revoke()` through `_save_role_state()` (`maintenance/ui/window_node_actions.py:88-116,159-169`), but does not remove trust/grants, invalidate the provider, cancel node resources, or remove registry visibility. Those operations remain in the separate `_revoke_trusted_node()` path (`maintenance/ui/window_node_actions.py:454-510`). The plan requires Revoke to invalidate the provider, remove cluster visibility, and require a new invite (`docs/plans/2026-09-11-coordinator-worker-roles.md:425-430`). This is established by repo contract and call paths, not external documentation: the visible control can persist a revoked role while leaving the node trusted and visible.

**Dashboard footer: no confirmed defect.** `dashboard_meta_frame` is packed after `cards_container` (`maintenance/ui/dashboard_page.py:141-150,182-200`), with refreshed time on the left and target status on the right. `render_selected_node()` updates target status and refreshed time and resets refreshed/scanned/health labels when no snapshot exists (`maintenance/ui/window_node_runtime.py:241-277`). The dashboard-focused portion of current validation passed; this is source and fake-widget evidence, not live Tk geometry evidence. No direct assertion of the frame hierarchy was found.

**P2/P3 conditional residual: `max_bytes` is logical payload accounting, not a physical SQLite/disk cap.** `append()` compares caller-supplied `encoded_size`, and `status().bytes_used` sums that field (`maintenance/components/cluster_storage.py:208-264,336-350`). A fresh temporary probe with `max_bytes=1` and `encoded_size=0` returned `cap=True`, `logical=0`, and a 28,672-byte database file. SQLite's official limits describe page/file limits and storage consumed by tables and indexes, so a physical hard-budget interpretation is unsupported. This is P2 only if the requirement promises physical bytes; otherwise it is P3/contract clarification. No ordinary declared-payload bypass or disk exhaustion was reproduced. The code uses the default rollback journal (`maintenance/components/cluster_storage.py:154-158`), so WAL `-wal`/`-shm` guidance is a contrast and limitation, not evidence about this implementation.

**Unconfirmed semantic concerns:** `can_promote()` allows exact lease expiry (`maintenance/components/cluster_roles.py:242-258`), but the plan does not define endpoint inclusivity. `import_batch()` performs prior read, gap insert, and append in separate connections (`maintenance/components/cluster_storage.py:266-311`); SQLite documents one writer and transaction boundaries, but no deterministic stale-gap race was reproduced. Neither is a confirmed bug.

**Official references and applicability:**

- Python JSON: https://docs.python.org/3/library/json.html#infinite-and-nan-number-values. Default decoding accepts `NaN`/infinity; `parse_constant` can reject them. This explains the test input, not an application defect after the current parser filters it.
- Python `sqlite3.connect()`: https://docs.python.org/3/library/sqlite3.html#sqlite3.connect. `timeout` waits for locked tables; it does not enforce a storage-size budget.
- Python HMAC: https://docs.python.org/3/library/hmac.html#hmac.compare_digest. Timing-resistant digest comparison is not authorization or lifecycle validation.
- SQLite limits: https://sqlite.org/limits.html#max_database_size. Database capacity is page/file based and distinct from application payload totals.
- SQLite WAL: https://sqlite.org/wal.html#concurrency. WAL has one writer and auxiliary files, but this implementation does not enable WAL.

**Evidence and limitations:** From `/home/btn17/Downloads/exp`, `python3 -m unittest tests.test_cluster_roles tests.test_cluster_roles_persistence tests.test_cluster_storage tests.test_cluster_failover tests.test_cluster_adversarial tests.test_remote_contract tests.test_remote_security tests.test_cluster_page tests.test_nodes_connections_page tests.test_dashboard_ui -q` passed 138 tests. Fresh read-only probes returned `cap=True logical=0 physical=28672`, `invite_parser_filters_nonfinite=True`, and `duplicate_active_coordinator_guard=True`; source inspection confirmed `cluster_revoke=controller._revoke_node` and the separate trust-cleanup path. No application code or normal tests were modified. No live Tk click/display geometry, TLS/two-installation exchange, restart/power-loss recovery, native macOS/Windows run, OS disk-full test, wheel verification, or forced gap race was run. `ruff check .`, `ruff format --check .`, `pyright`, `mypy --ignore-missing-imports .`, and LR commands were unavailable and are not passing evidence. Official docs cannot validate this application's authorization, provider cleanup, persistence ordering, footer behavior, or failover requirements; those conclusions rely on exact current-tree paths, the plan contract, and local probes. Registered BugGuard agents were unavailable; this is the requested portable fallback Agent 4 review, not a registered-agent result.

## Agent 5 evidence audit

### Scope and method

Portable fallback Agent 5 audit. I read this complete artifact through all four opposition sections, the current working-tree diff, the approved plan/design, `docs/CLUSTER_ROLES_FAILOVER_2026-09-11.md`, relevant production callers/callees, tests, and official Python/SQLite documentation. This audit changed no application code, normal tests, opposition section, or main-auditor section. Only this Agent 5 section is being replaced.

### Phase 1: Evidence reconciliation

The prior Agent 5 text was stale. Current source wires the Cluster-page callback to `_revoke_node` (`maintenance/ui/window_pages.py:146-154`). `revoke_node()` obtains a `RoleState` through `_role_state()` and calls `revoke()`, then `_save_role_state()` persists the assignment (`maintenance/ui/window_node_actions.py:80-116,159-169`). The separate `_revoke_trusted_node()` path performs provider invalidation, grant/trust removal, cancellation, and registry cleanup (`maintenance/ui/window_node_actions.py:454-510`); the Cluster-page role-revoke path does not call it.

Current opposition findings that survive reconciliation:

- **P1 confirmed:** Cluster-page Revoke persists role revocation but does not invalidate the provider, remove `trusted_nodes`/`peer_grants`, cancel active node resources, or remove registry visibility. The plan explicitly requires all of those lifecycle effects (`docs/plans/2026-09-11-coordinator-worker-roles.md:425-430`). This is a real authority-bearing control defect, not merely a UI wording issue.
- **P2/P3 contract residual:** `CoordinatorTimeline` enforces and reports `encoded_size`, not SQLite pages, the database file, journal/WAL sidecars, or filesystem allocation (`maintenance/components/cluster_storage.py:208-264,336-350`). With `max_bytes=1`, `encoded_size=0`, append was accepted, logical bytes were `0`, and the SQLite file was `28672` bytes. Severity is P2 only if the requirement means physical bytes; otherwise P3 documentation/contract narrowing.
- **Unconfirmed:** exact lease-expiry inclusivity (`can_promote()` returns true at equality) and the split prior-read/gap-insert/append race remain contract/race uncertainties; neither reproduced a confirmed defect.
- **Fixed or disproven:** malformed invite expiry is filtered by `_parse_invites()` (`maintenance/cluster.py:857-874`), duplicate active Coordinator records are normalized by `_parse_roles()` (`:801-836`), non-finite epochs are rejected (`maintenance/components/cluster_roles.py:78-87`), promotion storage/import and failed invite-save rollback are present, and `resume_worker` is in the protocol validator. These claims must not remain as residual bugs.

### Phase 2: Exact validation and counter-tests

Run from `/home/btn17/Downloads/exp`; probes used temporary local state only and no live peer, external service, process termination, or destructive action. Counter-test results were: split-brain `rejected`; worker role escalation `rejected`; stale remote epoch `rejected` with zero handler calls; invite replay `InviteExpiredError`; revoked reassignment `rejected`; standby overflow retained only the newest batch; late snapshot epoch `rejected`; Cluster-page revoke wiring `True`; role-revoke provider cleanup `False`; trust-revoke provider cleanup `True`; exact-expiry eligibility `True`. A fresh cap probe accepted `encoded_size=0` under `max_bytes=1`, reported logical bytes `0`, and produced a 28,672-byte SQLite file.

```bash
python3 -m unittest tests.test_cluster_roles tests.test_cluster_roles_persistence tests.test_cluster_storage tests.test_cluster_failover tests.test_cluster_adversarial tests.test_remote_contract tests.test_remote_security tests.test_peer_connection tests.test_nodes_connections_page tests.test_cluster_page tests.test_diagnostics tests.test_diagnostics_page -v
```

Observed: `Ran 130 tests in 6.816s; OK`.

```bash
python3 -m unittest discover -s tests -q
```

Observed: `Ran 1228 tests in 51.987s; OK`.

```bash
python3 -m compileall -q maintenance tests && git diff --check
./install/verify.sh
```

Observed: compile/diff exit 0; wheel verification passed with `contents OK: 91 members`, `SHA256SUMS OK`, SHA256 `d87ca667be4c7984865d4c5b5b5a9300951d6c3d23e38db3d67b4fed24e19020`.

```bash
python3 -m unittest tests.test_live_tk_resize -v
python3 -m tests.dump_ui > /tmp/patch-20260911-001-ui.json
```

Observed: live Tk resize suite `Ran 8 tests ... OK`; the deterministic UI dump completed and contained the dashboard, settings, preferences, nodes, cluster, thermals, and diagnostics pages. The dashboard structure places `dashboard_meta_frame` after `cards_container`; the target-status and refreshed labels are both updated by the selected-node render path. The direct `python3 tests/dump_ui.py` form was not usable because it omits the repository root from the import path (`ModuleNotFoundError: No module named 'tests'`); the module form is the valid rerun.

Unavailable gates, not passes:

```text
ruff check .                         -> ruff: command not found
ruff format --check .                -> ruff: command not found
pyright                              -> pyright: command not found
mypy --ignore-missing-imports .      -> mypy: command not found
./lr --list                          -> ./lr: No such file or directory
./lr impact                           -> ./lr: No such file or directory
```

### Phase 3: Sibling and official-evidence search

Sibling searches covered `PATCH-20260910-002-review.md`, `PATCH-20260909-009-review.md`, `PATCH-20260908-003-review.md`, the fallback assurance review, and the active bug-hunt index. Repository queries were `upload_snapshot(`, `upload_standby_batch(`, `revoke_worker`, `role_assignments`, direct `sqlite3` access, `_start_peer_listener`, `selected_context`, and executor/scheduler creation. They confirm the current listener uses the local registry context (`maintenance/ui/window_discovery.py:101-114`), one existing `ThreadPoolExecutor` owner, SQLite access confined to the storage/persistence surfaces, and no new arbitrary RPC or destructive-action route.

Official checks used:

- Python JSON documentation: https://docs.python.org/3/library/json.html#infinite-and-nan-number-values. Default decoding accepts `NaN`/infinity, while the current invite parser catches the resulting non-finite `InviteRecord` construction error; this supports the disproval of the stale persistence claim.
- Python sqlite3 documentation: https://docs.python.org/3/library/sqlite3.html#sqlite3.connect. Timeout and transaction behavior support bounded lock handling, not physical size enforcement.
- SQLite limits: https://sqlite.org/limits.html#max_database_size. Database size is page/file based, distinct from application payload accounting.
- SQLite WAL: https://sqlite.org/wal.html#concurrency. WAL has one writer and additional `-wal`/`-shm` files, supporting the cap distinction and the unforced race uncertainty.
- Python HMAC documentation: https://docs.python.org/3/library/hmac.html#hmac.compare_digest. `compare_digest()` supports timing-resistant comparison only; it does not establish role lifecycle correctness.

Required structured-search record: official queries were `Python json NaN parse_constant official docs`, `Python sqlite3 connect transaction timeout official docs`, `SQLite WAL auxiliary files concurrency official docs`, and `Python hmac compare_digest official docs`; repo queries were `upload_snapshot(`, `upload_standby_batch(`, `revoke_worker`, `role_assignments`, `sqlite3`, `_start_peer_listener`, and `selected_context`; LR query was `./lr --list`/`./lr impact` (unavailable); issue-tracker and Stack Overflow searches were not needed. The evidence changing the decision was the fresh revoke-path probe, parser source/probes, fencing/role counter-tests, local-context source check, physical-cap probe, and live/deterministic UI evidence. Official docs support SQLite transaction/locking, file/page-size, and HMAC comparison semantics; they do not prove this application's role lifecycle or UI contract.

### Phase 4: Lifecycle, drift, and uncertainty map

| Boundary | Assessment |
| --- | --- |
| Role constraints, expiry fencing, promotion timing, return fencing | Fixed for covered pure/local paths; live multi-installation behavior untested |
| Upload caller reachability and promotion import | Prior findings disproven/fixed; live transport and cadence untested |
| Auth, source/target/cluster checks, invite target binding, resume, revoked reassignment | Fixed for tested paths |
| Cluster-page revoke lifecycle | **P1 residual**, trust removal is not persisted role revocation |
| Persisted invite and role invariants | Current parser safely filters non-finite invites and duplicate active Coordinators; no residual from those prior claims |
| Persisted epoch numeric validity | Epoch constructor rejects non-finite values; prior epoch-acceptance claim is stale |
| Storage cap | **P2/P3 contract residual**, logical accounting is not a physical disk bound |
| Import gap atomicity | Uncertainty only; race not forced |
| Static/type/LR/release-platform evidence | Wheel verified; live Tk/UI evidence passed; Ruff, Pyright, Mypy, LR, native platforms, restart/power-loss, OS disk-full, and two-node exchange unavailable |

### Phase 5: Conservative recommendation

**Recommendation: do not accept the patch as-is.** Correct the P1 Cluster-page Revoke operation so the documented role revocation and trust/provider cleanup are one coherent, persisted lifecycle, then add a composition-level regression test proving role, grants, provider, registry visibility, and cancellation outcomes. Explicitly define `max_bytes` as logical payload accounting or implement a physical SQLite/journal/filesystem budget; current evidence does not justify a hard physical-cap claim.

Do not reintroduce stale promotion-storage, failed-save invite, absent upload-caller, unreachable `resume_worker`, wrong-target invite, revoked-reassignment, non-finite epoch, or dashboard-projection findings. Keep import-gap atomicity and exact-expiry semantics as unconfirmed uncertainties, not bug claims. Before release, rerun the unavailable static/type/LR gates when tools are supplied and add live two-node/TLS, restart/power-loss, native-platform, OS disk-pressure, and forced gap-race evidence. The registered BugGuard topology is unavailable; this is a durable portable fallback Agent 5 audit, not a registered-agent result.

## Main auditor rebuttal

The latest opposition sections correctly identify the logical-versus-physical storage-cap distinction and the unavailable static/native validation. Their remaining Cluster-page Revoke finding is stale against the current tree: `maintenance/ui/window_pages.py:153` routes the action to `_revoke_node`, and `maintenance/ui/window_node_actions.py:159-170` first persists `RoleState.revoke()` and then calls `revoke_trusted_node()`, which invalidates the provider, cancels node work, removes grants/trust, and clears registry visibility. The dashboard footer move is also covered by rendered structure evidence: the target-status label is now in the same bottom metadata frame as “Not refreshed yet,” packed right with a bounded wrap width.

The implementation is functionally complete for the requested role, failover, storage, diagnostics, and dashboard placement behavior. Remaining acceptance limits are explicit: the configured storage cap is logical `encoded_size` accounting rather than a physical SQLite/filesystem budget, and `ruff`, `pyright`, `mypy`, LR checks, native platform runs, and live multi-installation failover are unavailable in this environment.

## Final patch decision

needs more evidence

## Remaining risk

Static/type/format tools are unavailable locally. Native macOS/Windows and live multi-installation runtime validation are unavailable. The storage cap should not be described as a physical disk cap until that contract is either narrowed in documentation or implemented with platform-safe SQLite/filesystem accounting.
